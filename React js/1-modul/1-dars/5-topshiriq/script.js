//numbers map
const numbers = new Map();

numbers.set('a', 10)
numbers.set('b', 20)
numbers.set('c', 30)
// console.log(numbers);

//map values
console.log(numbers.values());

//map keys
console.log(numbers.keys());

//map size
const mapSize = numbers.size;
console.log(mapSize);